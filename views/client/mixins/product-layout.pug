include product-item.pug 

mixin product-grid1(products)
  .row
    each product in products 
      +product-item1(product)

mixin product-grid2(products)
  .row
    each product in products 
      +product-item2(product)
